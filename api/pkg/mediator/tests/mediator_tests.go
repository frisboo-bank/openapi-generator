package mediator_test

import (
	"testing"
)

func TestMediatorSendMatchesHandler(t *testing.T) {
	// rapid.Check(t, func(t *rapid.T) {
	// 	mediator := mediator.NewMediator(&models.MediatorOptions{}, nil)
	// },
}
